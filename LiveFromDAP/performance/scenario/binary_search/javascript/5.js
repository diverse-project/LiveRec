['a', 'b', 'c', 'd', 'e', 'f'], 'd';undefined;true
function binary_search(arr, target){
    var low = 0;
    var high = arr.length - 1;

    var mid = Math.floor((low + high) / 2);
    var value = arr[mid];
}