new char[]{'a','b','c','d','e','f'}, 'd';0;true
public class BinarySearch {
    public static int binarySearch(char[] array, char key) {
        int low = 0;
        int high = array.length - 1;

        int mid = (low + high) / 2;
        return 0;
    }
}